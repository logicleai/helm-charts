# Logicle CE Helm Chart

## Prerequisites

- A working Kubernetes cluster
- `kubectl` and `helm` installed on the client system for Helm chart installation

## Installing Logicle

  1. Add the Helm Repository

      ```bash
      helm repo add logicleai https://helm.logicle.ai/
      ```